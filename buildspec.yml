version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18

  build:
    commands:
      - echo "CodeBuild triggered successfully"
      - echo "CI/CD pipeline completed"

artifacts:
  files:
    - '**/*'